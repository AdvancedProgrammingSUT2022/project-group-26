package models.Units.Combat;

public enum UnitTypeEnum {
    archery("archery"),
    mounted("mounted"),
    recon("recon"),
    civilian("civilian"),
    melee("melee"),
    siege("siege"),
    gunpowder("gunpowder"),
    armored("armored");

    UnitTypeEnum(String name) {
    }
}